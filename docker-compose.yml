version: "3.9"

services:

  dbserver:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    image: vetflow-db-image
    container_name: vetflow-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: vetflowdb
      POSTGRES_USER: vetflow
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - vetflow-network

  app:
    build:
      # Contexto = pasta com o código-fonte do vetflow-java clonado localmente.
      # Ver README "Como Executar": clone o vetflow-java para ./build-app
      # e copie Dockerfile + db-patches/ para dentro dela antes do build.
      context: ./build-app
      dockerfile: Dockerfile
    image: vetflow-api-image
    container_name: vetflow-app
    restart: unless-stopped
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dbserver:5432/vetflowdb
      SPRING_DATASOURCE_USERNAME: vetflow
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
    ports:
      - "8080:8080"
    depends_on:
      - dbserver
    networks:
      - vetflow-network

volumes:
  db-data:
    name: vetflow-db-data

networks:
  vetflow-network:
    external: true
