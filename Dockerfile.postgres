FROM postgres:16

# POSTGRES_PASSWORD obrigatorio em runtime (sem default aqui de proposito)
ENV POSTGRES_DB=vetflowdb
ENV POSTGRES_USER=vetflow

# Sem script de init: o schema e criado automaticamente pelo Flyway quando
# a API conecta. script_bd.sql no repo e so documentacao do DDL resultante.

EXPOSE 5432
