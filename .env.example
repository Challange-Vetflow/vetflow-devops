# Copie este arquivo para ".env" (fora do git, já no .gitignore) e troque a senha.
# Uma única variável, usada tanto pelo Postgres quanto pela API, então nunca
# ficam dessincronizadas.
DB_PASSWORD=troque-esta-senha
